<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <%= link_to("edit", "/users/#{@user.id}/edit") %>
      <img src="<%= "/user_images/#{@user.image_name}" %>">
    </div>
  </div>
</div>